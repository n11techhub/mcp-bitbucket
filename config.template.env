# Bitbucket Configuration Template
# Copy this file to .env and update with your actual values

# Bitbucket Configuration
# Your Bitbucket Server/Data Center URL
BITBUCKET_URL=https://your-bitbucket-server.com

# Personal Access Token for Bitbucket authentication
# Generate this from your Bitbucket profile -> Personal Access Tokens
BITBUCKET_TOKEN=your_personal_access_token_here

# Optional: Default project key for repositories
# If not set, project key must be specified in requests
# BITBUCKET_PROJECT_KEY=YOUR_PROJECT

# Transport Configuration
# Enable HTTP streaming transport (default: false, uses stdio)
ENABLE_HTTP_TRANSPORT=false

# HTTP transport configuration (only used if ENABLE_HTTP_TRANSPORT=true)
MCP_HTTP_PORT=3001
MCP_HTTP_ENDPOINT=/stream

# API key for HTTP transport authentication (recommended for production)
# Generate a secure random key: openssl rand -hex 32
# MCP_API_KEY=your_secure_api_key_here

# Logging Configuration
# Log level: error, warn, info, verbose, debug, silly (default: info)
# LOG_LEVEL=info

# Log file path (optional, logs to console if not set)
# LOG_FILE=/var/log/mcp-bitbucket.log 